import { BlockOverrides } from 'baseui/block';

export const MINI: BlockOverrides = {
  Block: {
    style: {
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      overflow: 'hidden',
      height: '100%',
    },
  },
};
