import { BlockOverrides } from 'baseui/block';

export const BLOCK: BlockOverrides = {
  Block: {
    style: {
      display: 'flex',
      width: '100vw',
      height: '100vh',
      overflow: 'hidden',
    },
  },
};
