import { ToastOverrides } from 'baseui/toast';

export const NOTIFICATION: ToastOverrides = {
  Body: {
    style: {
      width: 'auto',
      marginTop: '20px',
      marginBottom: '30px',
    },
  },
};
